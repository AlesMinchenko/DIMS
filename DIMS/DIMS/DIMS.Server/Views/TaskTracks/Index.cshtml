@model IEnumerable<HIMS.EF.DAL.Data.TaskTrack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TrackDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrackNote)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserTask.UserTaskId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TrackDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrackNote)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserTask.UserTaskId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskTrackId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskTrackId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskTrackId })
        </td>
    </tr>
}

</table>
